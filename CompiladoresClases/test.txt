let
    var y:int;
    var w:char;
    const PI~'a';
    const PI~2+3+4;
    void met(x:int)
        x := 20
in
begin
    let
        var local:int;
        var local:char;
        var z:int
    in
    begin
        y := local;
        y := w
    end;
    y := w;
    met(y)
end