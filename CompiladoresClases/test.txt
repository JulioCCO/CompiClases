let
    var y:int;
    var w:char;
    void met(x:int)
        x := 20
in
begin
    let
        var local:int;
        var local:int;
        var w:int
    in
    begin
        y := local;
        y := w
    end;
    y := w;
    met(y)
end